# 裁決紀錄（codex review 駁回與範圍裁定）

主代理對 codex adversarial review findings 的駁回與延後裁決。每條附理由；
除非能證明理由在事實上錯誤，否則後續輪次勿重提。接續編號。

## #1（web-v4 P3a r4-2，2026-07-26）：LLM 輸出的 lone surrogate 維持「拒絕＋修正重試」，不改洗白保留

**Finding 主張**：`ToolSummaryResult._sanitize` 先走嚴格 UTF-8 的 `_coerce_str`，
模型回傳含 `\ud800` 的合法 JSON 會在驗證層被拒，r3 加的寫入端 surrogate 洗白
（`write_tool_meta`）對 LLM 路徑不可達；建議改為洗白保留（U+FFFD），讓 regenerate
不至於 502、install hook 不至於只留 placeholder。

**駁回理由**：reject-vs-replace 的邊界是 repo 的既定契約，`llm_log._utf8_safe`
docstring 明文界定——**尚未落地的資料在驗證層拒絕**（`memory_ai._coerce_str`：
raise → 一次修正重試 → 502），**已經發生、必須記錄的資料在記錄層替換**（llm_log
對 attempt bodies 的 U+FFFD 替換）。`ToolSummaryResult` 是 LLM 輸出的驗證層，
與 capture／enrich／assist-update 的所有欄位同側：模型輸出含 lone surrogate
就是 invalid output，修正重試是設計好的恢復路徑，與全 repo 一致。r3 的讀寫端
洗白針對的是**手編 sidecar**（繞過我們的寫入端，事後只能替換不能拒絕）——兩側
各在正確的位置。把 summary 特殊化為「模型輸出也洗白」會讓同一種模型缺陷在
不同 workflow 得到不同語意，換取的只是病態 gateway 情境下的邊際 UX。

**寫入端 scrub 的定位**：`write_tool_meta` 的 surrogate 洗白保留，作為非 LLM
呼叫端（`set_summary_status` round-trip、未來程式化寫入）的 defense-in-depth。

## #3（web-v4 P3a r5-3，2026-07-26）：併發 regenerate 的 AI 日誌連結撞名——沿用 llm_log 既有 accepted imprecision，不加 single-flight

**Finding 主張**：同步 regenerate 無 single-flight，兩個併發 regenerate 完成時序
交錯時，`last_record_id_for_workflow("tool_summary")` 可能把 B 的紀錄 id 存進
A 的 sidecar，除錯連結指向別的生成。

**駁回理由**：這正是 `llm_log.last_record_id_for_workflow` docstring
（llm_log.py:1071-1076）**已明文裁定**的已知不精確：「TWO interactions of the
same workflow overlap → whichever finished last wins…a debugging link to a
simultaneous session in this single-user local tool — still useful, never
harmful — and does not justify rewriting the generate_structured contract」。
regenerate 與 install 是同一類（單人本機工具、debug 連結、值只差一個相鄰
session），同類同準繩。為此加 regenerate 專屬 in-flight 旗標或改
`generate_structured` 回傳 id，都是為單一消費者重寫既裁決邊界——改名教訓
（「防禦性功能的失敗模式比它解決的問題難時，先確認問題真的存在」）直接適用。
單一使用者要在一個 LLM 往返窗內對兩個工具同時按重新產生才會觸發，後果是
連到隔壁 session 的除錯紀錄，仍可用、無害。除非證明推理錯誤，勿重提。

## #2（web-v4 P3a r4-3 範圍裁定，2026-07-26）：`InstallResult._sanitize` 的同類 event-loop redaction 掃描——有意識延後

r4-3 接受修復的是 `ToolSummaryResult` 路徑（redact＋cap 移出 validator、進
threadpool 的 store 步驟）。`InstallResult._sanitize` 在 installer 路徑上有同類
的 validator 內 `redact_known_secrets`（P5/D36 起既有，早於本 phase），同樣在
event loop 上跑 `known_secret_values` 的目錄掃描。**不在本 phase 修**：屬既有
行為、installer 的 24 輪 session 本身就以分鐘計，一次 stat sweep 佔比可忽略；
記錄於此以免 overall review 誤判為遺漏。若日後把 installer 也搬 threadpool，
一併處理。

## #4（web-v4 P3a r8 範圍裁定，2026-07-27）：`_find_embedded_secret_file` 的 os.walk 靜默跳過不可讀子樹——既有面、有意識延後

r8-2 接受修復的是 r7 新增的 sidecar 剷除器（`onerror` fail-closed）。同一個
「`os.walk` 預設吞掉 scandir 錯誤 → 不可讀子樹被靜默跳過」性質也存在於
**P5/D36 既有的** `_find_embedded_secret_file`（H3 embedded-secret gate）：builder
可 chmod 000 一個子目錄讓該 gate 掃不到裡面的檔案。**本 phase 不修**：屬既有
面（早於 P3a）、且 D21 明文的信任邊界是「操作者只安裝自己信任的工具指示」，
H3 gate 是 belt-and-braces 而非主要防線；另外 P3b 的 revise 會 copytree 既有
套件，不可讀子樹屆時本來就會讓 copy 失敗浮上檯面。P3b 實作時一併評估是否
給該 gate 加同款 `onerror`。除非證明推理錯誤，勿在 53ef8b8..HEAD 範圍重提。

## #5（web-v4 P3a r10-1，2026-07-27）：tools_dir 整體搬移＋symlink 重建的信任錨漂移——D21 威脅模型範圍外，駁回

**Finding 主張**：builder 的 run_shell 可把整個 `<tools_dir>` 搬走、原路徑放
symlink，讓 `_verify_staging_root` 的 `base.resolve()` 信任錨跟著漂移、閘照樣
放行，隨後 strip／cleanup 對搬走的真 tools root 做破壞性操作。

**駁回理由**：D21（tools.py 模組 docstring）明文的信任立場——run_shell 以
**服務自身 uid** 執行、v1 **不做對抗性隔離**、「真正的信任邊界是操作者只安裝
自己信任的工具指示，不是 scrub」。能夠搬移整個 tools_dir 的行為者，直接
`rm -rf <tools_dir>` 比誘導我們的 cleanup 繞路刪除**更簡單**——加固間接路徑
對直接路徑敞開的同權限攻擊者提供零安全價值（改名教訓：防禦性功能的失敗模式
比它解決的問題難時，先確認問題是否真的存在）。r7–r9 的修復仍然正確：它們擋
的是「低成本竄改、甚至困惑的非惡意 builder」類（staging 內留檔案、staging 自
身被換），r10-1 則要求主動搬移 live tools root——該能力等級的行為者不需要我們
的程式碼幫忙。要真正封鎖錨點漂移只能走 fd-based traversal（openat/O_NOFOLLOW
全程 fd 相對操作），屬容器級隔離工程，D21 明確劃在 v1 範圍外。除非威脅模型
本身改變（D21 修訂），勿重提。

## #6（web-v4 P3b r7-2b，2026-07-27）：修訂期間操作者新建的憑證檔被 builder 讀走——D21 信任邊界下不可防，只封「不出貨」那一半

**情境**：一次 AI 修訂正在跑（builder session 以分鐘計），操作者在這段期間於
**正式套件目錄**（或任何該服務讀得到的地方）新建／編輯一個含憑證的檔案——包含
該套件的 `.env`。builder 的 `run_shell` 一句 `cat` 就能把它印出來，而那個值
**當下不在遮蔽器的已知集合裡**：`register_inflight_secret` 登記的是**開工那次讀取**
解析出來的值，`known_secret_values` 的快取也只在掃描時看得到當時的檔案。於是那段
原文會原樣進入該輪的提示、AI 日誌 attempt bodies，甚至總結。

**為什麼不可防**：`llm_log` 是**寫入當下**遮蔽的——紀錄一旦落地，事後才知道的值
無法回頭遮蔽既有紀錄；而要在讀取當下就遮，遮蔽器必須先知道那個值，這正是循環。
唯一的實質解法是不讓 builder 讀到未登記的檔案，也就是**把 run_shell 關進沙箱**——
D21（`tools.py` 模組 docstring）明文把這件事劃在 v1 之外：run_shell 以**服務自身
uid** 執行、刻意不做對抗式隔離，「真正的信任邊界是操作者只安裝自己信任的工具指示」。
在那個信任邊界底下，builder 讀得到服務讀得到的一切，而我們的遮蔽器只認得我們登記過
的東西——這不是實作缺口，是威脅模型的直接推論。（同理，操作者在別處新建的憑證檔
本來就不在本系統管轄內，這裡只是 revise 讓它多了一個被讀出來的時機。）

**r7-2 已經封住的那一半（不要因為上面那段就以為什麼都沒做）**：
`_preserve_env_file` 在複製前會**重讀它即將複製的那個來源**、對那些**實際會出貨的
位元組**重跑同一套 `.env` 政策（拼法閘＋長度下限＋位元組上限），不過就以同一組
category-only 錯誤拒絕換裝；同時把新看到的值**在複製之前**登記成進行中的秘密（並記進
session 自己的清單，由既有的 `finally` 丟棄），讓換裝後的總結／sidecar 路徑有遮蔽。
換句話說：**「絕不出貨閘門會拒絕的東西」是可封的，而且已經封了**；不可封的是
**session 進行中的那次讀取**。

**定位**：既不誇大成漏洞——觸發它需要操作者在自己啟動的修訂進行中，親手把新憑證放進
同一台機器上的套件裡，而 D21 的信任邊界本來就假設操作者信任自己安裝的工具；也不淡化成
非問題——它會讓一個真實憑證以未遮蔽的原文留在 AI 日誌裡。

**操作指引（面向使用者，已寫進 `backend/README.md` 與 `docs/tool-calling.md`）**：
**某個套件的 AI 修訂正在進行時，不要新增或編輯該套件的 `.env`**；要換憑證就等修訂結束
（或先取消）再改。除非 D21 的威脅模型本身改變（改成隔離 run_shell），勿重提。

## #7（web-v4 P4 r5-3，2026-07-27）：同名＋同 description 重裝造成的 draft 錯置——修可修的部分，其餘需後端 instance id，本輪不做

**Finding 主張**：`description` 相同時舊新工具共用 summary query key 與 row key，於是
面板不 remount、未送出的意見會留在輸入框，按下送出就打到新工具身上；而且「重新整理」
只 refetch 清單、根本不會重抓 summary，所以連 warning 都不會出現。

**已修的部分**：「重新整理」現在同時 invalidate `["tool-summary"]` 前綴，於是任何一次
刷新都會按**名稱**向伺服器重問，面板不再可能停在舊工具的總結上（r5 修法）。

**未修、書面接受的殘留**：意見草稿仍會存活。要真正關閉它，row key 必須帶一個**真正的
實例身分**，而 `GET /api/tools` 目前沒有任何這種欄位（沒有安裝 id、沒有時間戳），
`description` 只是啟發式代理。加欄位是後端 API 變更，屬 P4 範圍外。

**為何接受**：觸發條件是「單人本機工具的使用者在分頁 A 打好意見未送出 → 在分頁 B（或
另一個行程）刪除該工具並以**同名**重裝一個 AI 自動產生的 description **逐位元組相同**
的不同工具 → 回到分頁 A 按送出」。而修訂本身是可逆的（工具包有備份語意、總結可重新
產生），後果不是資料損毀。**後續建議**：若日後 `GET /api/tools` 加上安裝時間戳或 id，
`toolInstanceKey` 應改用它，屆時 row/cache/job/ledger 四個消費端會自動一起收斂——
這正是 r4 把身分收斂成單一函式的目的。除非證明推理錯誤，勿在 P4 範圍重提。

## #8（web-v4 overall r9-3，2026-07-27）：toggle＋生成失敗會遺失「失敗那次 session」的日誌連結——修它需要用一個已證明不能回答該問題的身分，故不修

**Finding 主張**：r8 的 origin-only 提前寫入成功之後，失敗分支仍以 round trip 之前捕捉
的 manifest identity 去寫 placeholder。若期間發生啟用切換，`tool.json` 被就地改寫、
identity 位移，placeholder 就會被守衛拒絕，磁碟上只剩 `llm_log_id=None` 的 origin-only
側檔——那次**失敗**的總結 session 的 AI 日誌連結沒有落盤，事後也無從恢復（job 上的連結
指的是 builder session，而 regenerate 只會產生新的 session）。

**主張屬實**，程式碼與失敗情境都已查證。

**為何不修**：placeholder 相對於「已經在磁碟上的 origin-only 側檔」唯一新增的資訊就是
那個 `llm_log_id`。要讓這次寫入通過，必須讓守衛在「同一個套件、只是 manifest 被改寫」
與「換成了另一個套件」之間做出區分——也就是改用 `directory_identity` 來回答「還是同一個
套件嗎」。**D40 r5 已經證明那個問題不能用目錄身分回答**：刪除後同名重裝會**重用目錄
inode**，正是當初把兩個身分拆成兩種形狀、並讓 manifest identity 專責回答這一題的理由。
換句話說，修這條 P3 的代價是推翻本子系統中被最仔細裁決過的那條規則，用來換一個除錯
連結。

**觸發條件與後果的比例**：需要「安裝 promote 之後、總結生成期間操作者剛好切換啟用」
**且**「同一段時間內總結生成失敗」兩件事同時成立；後果是少一個除錯連結，不是資料損毀
——origin 已由 r8 保住、總結文字可重新產生、安裝本身正確地維持 succeeded。

**後續建議**：若日後 `GET /api/tools` 取得真正的實例身分（見 #7），或 llm_log 改為可
依 workflow＋套件查詢歷史 session，這條可一併收斂。除非證明上述推理錯誤，勿重提。
